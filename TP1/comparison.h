

// This is the function to call when comparing the newton approxima6on to the real root.
float comparison(float initial_value, float root);
// This function returns the positive value of comparison
float comparison_absolutie(float initial_value, float root);

